# Copie este arquivo para .env:   cp .env.example .env
# O .env NÃO é versionado (veja o .gitignore). Configuração fica fora do código:
# o mesmo código roda em qualquer máquina — só o .env muda.

DATA_RAW_PATH=data/raw
DATA_BRONZE_PATH=data/bronze
DUCKDB_PATH=data/analytics.duckdb
LOG_LEVEL=INFO
