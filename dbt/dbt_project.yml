name: tribunal
version: "1.0.0"
profile: tribunal

model-paths: ["models"]

models:
  tribunal:
    silver:
      # Silver materializada como Parquet em data/silver/ (medallion visível!).
      +materialized: external
      +format: parquet
    gold:
      # Gold materializada como Parquet em data/gold/.
      +materialized: external
      +format: parquet

